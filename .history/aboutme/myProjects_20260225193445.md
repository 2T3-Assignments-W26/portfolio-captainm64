
# Weather Array Project

The weather array project was made during my first semester, and it involves typing the amount of rain per each month of the year, and then the program automatically calculates the total rainfall, average rainfall and highest and lowest rainfall in the whole year.

## Technologies Used 

Visual Studio 2022

## Code Snippets

# High and Low Code
 static Int32[] HiLo(Int32[] haverage)

 {
     Int32 Hi = 1;
     Int32 Lo = 999;
     Int32 Hiword = 0;
     Int32 Loword = 0;


     for (int i = 0; i < ARRAY; i++)

     {
         if (haverage[i] > Hi)
         {

             Hiword = i;
             Hi = haverage[i];

         }

         if (haverage[i] < Lo)
         {

             Loword = i;
             Lo = haverage[i];

         }
     }


     monthstowords(Hiword);

     Console.WriteLine("The highest rainfall in the entire year was during " + name);

     monthstowords(Loword);

     Console.WriteLine("The lowest rainfall in the entire year was during " + name);
     return haverage;
 }

 # Total Rainfall Calculator

             static Int32 Calculate(Int32[] dyarray)
            {
                Int32 dtotal = 0;

                for (int i = 0; i < ARRAY; i++)
                {
                    dtotal = dtotal + dyarray[i];
                }

                Console.WriteLine("The total rainfall throughout the year was " + dtotal);

                return dtotal;

            }
        

## Screenshots from the project

 ![image](/aboutme/Screenshot%202026-02-25%20183139.png)

 ![image](/aboutme/Screenshot%202026-02-25%20190252.png)

## What I learned

This is where I learned how to use arrays properly, as well as smaller things revolving calling functions properly.


# Final Game Project: Get that @!!!!

This was my final game project for Programming 1, and the way it works is that you control a "*" symbol, and you have to find and touch an "@" before the timer runs out.

## Technologies Used 

Visual Studio 2022

## Code Snippets

# The main Draw function
             void Draw()
            {

                Console.SetCursorPosition(OldMeX, OldMeY);
                Console.Write(Empty);
                Console.SetCursorPosition(MeX, MeY);
                Console.Write(Player);
                Console.SetCursorPosition(CoinX, CoinY);
                Console.Write(Coin);
                Console.SetCursorPosition(Console.WindowTop, Console.WindowLeft);
                Console.Write(FormatHUD(Score, Timer));
            }

 # Total Rainfall Calculator

             static Int32 Calculate(Int32[] dyarray)
            {
                Int32 dtotal = 0;

                for (int i = 0; i < ARRAY; i++)
                {
                    dtotal = dtotal + dyarray[i];
                }

                Console.WriteLine("The total rainfall throughout the year was " + dtotal);

                return dtotal;

            }
        

## Screenshots from the project

 ![image](/aboutme/Screenshot%202026-02-25%20183139.png)

 ![image](/aboutme/Screenshot%202026-02-25%20190252.png)

## What I learned

This is where I learned how to use arrays properly, as well as smaller things revolving calling functions properly.